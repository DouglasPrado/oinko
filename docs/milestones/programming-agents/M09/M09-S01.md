# M09-S01 — Executar bateria determinística e falhas injetadas

**Status:** in_progress — suíte de validação executada (gates agregados, suítes por pacote, Docker real e Playwright 39/39) com skipped listados e relatório separado por ambiente; a matriz de VALIDATION.md não está totalmente coberta (ex.: pausa durante patch/publicação) e as auditorias de segredos/correlação em todas as execuções e de amostra de traces não foram feitas.

**Milestone:** [M09 — Validação integrada e adoção gradual](MILESTONE.md)
**Dependências:** [M02-S05](../M02/M02-S05.md), [M03-S06](../M03/M03-S06.md), [M04-S04](../M04/M04-S04.md), [M05-S04](../M05/M05-S04.md), [M06-S04](../M06/M06-S04.md), [M07-S04](../M07/M07-S04.md)
**Requisitos:** [R02](../REQUIREMENTS.md#r02), [R08](../REQUIREMENTS.md#r08), [R09](../REQUIREMENTS.md#r09), [R12](../REQUIREMENTS.md#r12), [R13](../REQUIREMENTS.md#r13), [R14](../REQUIREMENTS.md#r14), [R15](../REQUIREMENTS.md#r15)

## Resultado esperado

Executar bateria determinística e falhas injetadas. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

testes dos pacotes; Docker E2E; dashboard E2E. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [ ] Cobrir matriz VALIDATION.md: arquivos, concorrência, restart, controles, browser, GitHub e modelos.
- [x] Executar checks agregados e suítes reais de Docker/dashboard pertinentes.
- [x] Injetar falhas em fronteiras de efeitos externos e conferir recibos e deduplicação.
- [ ] Auditar segredos, isolamento e correlação completa em todas as execuções.

## Critérios de aceite

- [x] Todos os casos determinísticos passam; skipped são listados e impedem afirmar cobertura daquele cenário.
- [x] Restart durante comando/patch/build/push/PR não duplica efeito.
- [x] Não há violação de acesso entre bots, projetos, sessões ou repositórios.
- [x] Relatório separa teste automatizado, Docker real, provedor real e aceite humano.

## Validação e evidências exigidas

- [x] Executar comandos raiz e suites documentadas em VALIDATION.md.
- [ ] Auditar amostra de traces e falhas esperadas contra catálogo.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

validation_suite_started, validation_suite_finished; ambiente, SHA e skipped.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `be30ad6` (suíte de validação), `2a3b088` (`ValidationLog`), `835dcb5` (correção do E2E e pasta `validation-reports/`), `60fe1f9` (relatório ignora edições em `docs/`). Caminhos: `scripts/validation-suite.mjs`, `packages/agent-runtime/src/programming/validation.ts`, `docs/milestones/programming-agents/validation-reports/`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário, simulado, Docker real e Playwright.
- Evidências automatizadas: Relatórios em `validation-reports/`: `validation-60fe1f978cf9.md` (tudo aprovado; skipped fora do Docker: 1 SDK, 71 environments, 4 bots, 1 MCP; Docker environments 69 aprovados + 1 skipped; Docker worker+runner+harness 3/3; Playwright 39/39) e `validation-be30ad61fc88.md` (1 falha em `apps/dashboard/tests/e2e/workspaces.spec.ts`, corrigida em `835dcb5`); `packages/agent-runtime/tests/programming/validation.test.ts`: "never records a suite with skipped scenarios or failures as a pass", "keeps human decisions to operators with evidence, and refuses a milestone with pending items"; falhas injetadas: `packages/environments/tests/publication-flow.test.ts` "reconciles a timeout before the push and after the push without a second push or PR" e "recovers after a runner crash between push and receipt", `packages/environments/tests/workspace-extension.test.ts` "recovers a patch interrupted after its first write, finishing only what is pending", Docker `packages/bots/tests/programming.e2e.test.ts` "recovers after the worker is killed during a check without repeating the edit"; isolamento: `packages/bots/tests/pilot.test.ts`, `packages/mcps/oinko/tests/runs.test.ts` "acts only as its bot when scoped, without administrative tools or other bots’ runs", Docker `packages/environments/tests/browser.e2e.test.ts` "isolates cookies and storage between two bots, two runs and the docs context". Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/docker.test.ts tests/runner.e2e.test.ts tests/failures.e2e.test.ts tests/browser.e2e.test.ts tests/workspace.e2e.test.ts tests/publication-docker.e2e.test.ts` (em `packages/environments`); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/programming.e2e.test.ts tests/evaluation.e2e.test.ts` (em `packages/bots`); `pnpm exec playwright test` (em `apps/dashboard`; `programming.spec.ts` 11/11).
- Evidências reais / aceite de operador: Pendente — provedor real, GitHub App real, Telegram real e aceite humano aparecem como não executados nos relatórios.
- Limitações e riscos residuais: Skipped que impedem afirmar cobertura: "builds a Railpack subdirectory with custom build/start commands and serves it through Traefik" (Docker), "lets a bot create multi-repository worktrees, edit code, commit and enforce project boundaries in Docker" (bots; fora da suíte Docker), "prepares GitHub through MCP and serves the selected worktree over real Docker and HTTP" (MCP) e o teste de corrida do `MCPAdapter` (SDK). Casos da matriz ainda sem teste: pausa durante patch e publicação, desfecho de timeout na busca.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
