# M04-S01 — Configurar capacidades e políticas na dashboard

**Status:** completed — política durável por bot, dois bots novos pelo mesmo serviço, comandos/origens/credenciais/GitHub do projeto com round-trip, origem da política efetiva e aviso de snapshot, validação no serviço compartilhado testados por Playwright e unitário; aceite do operador na dashboard ainda não registrado.

**Milestone:** [M04 — Operação pela dashboard, Telegram e MCP](MILESTONE.md)
**Dependências:** [M00-S02](../M00/M00-S02.md), [M03-S01](../M03/M03-S01.md)
**Requisitos:** [R01](../REQUIREMENTS.md#r01), [R02](../REQUIREMENTS.md#r02), [R04](../REQUIREMENTS.md#r04), [R11](../REQUIREMENTS.md#r11), [R14](../REQUIREMENTS.md#r14)

## Resultado esperado

Configurar capacidades e políticas na dashboard. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

apps/dashboard bot/project settings; shared configuration service. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Criar configuração reutilizável de programação, retomada, autonomia e modelos por bot.
- [x] Configurar comandos por pacote, browser e repositórios por projeto.
- [x] Mostrar política efetiva, origem e necessidade de reinício quando aplicável.
- [x] Validar schema no serviço compartilhado usado por UI e MCP.

## Critérios de aceite

- [x] Criar segundo bot exige configuração, sem copiar implementação.
- [x] Permissões e capacidades são visíveis antes de iniciar trabalho.
- [x] Não há campo de teto financeiro aplicado por padrão ou implícito.
- [x] Salvar configuração inválida não altera parcialmente o bot.

## Validação e evidências exigidas

- [x] Testar E2E criação/configuração de dois bots e round-trip via serviço.
- [x] Testar alterações concorrentes e resumo de políticas efetivas.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

bot_configuration_changed, project_configuration_changed; diff redigido e ator.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `6c77375` (telas e rota de política), `140cb12` (modelos e validação antes de ativar), `c337590` (schema), `51b823d` (editor de projeto com revisão base, verificação do GitHub e E2E), `9c3d44f` (origem da política efetiva e aviso de snapshot; E2E de origens, credenciais e GitHub do projeto). Caminhos: `apps/dashboard/src/features/bots/programming-settings.tsx`, `apps/dashboard/src/features/projects/{programming-settings,project-editor}.tsx`, `apps/dashboard/src/app/api/programming/policy/route.ts`, `packages/bots/src/programming/models.ts`, `packages/bots/src/store.ts`.
- Revisão e ambiente testados: Revisão `9c3d44f`, relatório de validação em `validation-reports/` (`validation-9c3d44fb133d.md`: build, typecheck, lint, test, suítes por pacote, gates de IDs de piloto e de links internos, Docker real — runner/navegador 69 aprovados e 1 skipped (Railpack), worker+runner+harness 6/6, MCP Oinko 3/3 — e Playwright 48/48 aprovados; histórico em `validation-18edc367c33a.md`, `validation-60fe1f978cf9.md` e `validation-be30ad61fc88.md`, este com 1 falha em `workspaces.spec.ts` corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário e Playwright.
- Evidências automatizadas: `apps/dashboard/tests/e2e/programming-ops.spec.ts`: "shows where the effective policy comes from and that running work keeps its own" (o E2E "configures commands, browser and GitHub per project and tells installed from reachable" agora preenche e relê origens permitidas, credenciais de teste e repositório GitHub); `apps/dashboard/tests/e2e/programming-ops.spec.ts`: "configures two new bots with different programming policies through the same service", "configures commands, browser and GitHub per project and tells installed from reachable"; `apps/dashboard/tests/e2e/programming.spec.ts`: "configures a durable programming policy per bot and shows the effective policy", "validates the run model policy before activation and keeps it across reloads", "refuses anonymous access to runs and evidence"; `apps/dashboard/tests/e2e/bots.spec.ts`: "requires login and rejects cross-origin bot mutations"; `packages/bots/tests/models.test.ts`: "accepts different models per bot and rejects what cannot work", "refuses to save an invalid run policy, keeping the previous revision"; `packages/mcps/oinko/tests/bots.test.ts`: "rejects stale revisions, missing bots, unknown fields and invalid merged definitions without writing"; `packages/mcps/oinko/tests/runs.test.ts`: "updates the durable programming policy with a revision and records the capability change"; `packages/agent-runtime/tests/programming/policy.test.ts`: "defaults to disabled: migrating a bot never widens permissions and has no money cap"; `packages/bots/tests/pilot.test.ts`: "runs both in parallel with isolated queues, visibility and telemetry, and denies publication to the non-publisher". Comandos: `OINKO_DOCKER_TEST=1 pnpm exec playwright test` (em `apps/dashboard`; 48/48 na revisão `9c3d44f`, com `programming.spec.ts` e `programming-ops.spec.ts`); `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote).
- Evidências reais / aceite de operador: Pendente — aceite humano do operador na dashboard não registrado (nenhum checkbox desta story o exige).
- Limitações e riscos residuais: O aviso de reinício é texto fixo ("O worker do bot aplica mudanças após reiniciar"), sem detecção de quando o reinício é necessário. O conflito de revisão em edições concorrentes é testado no store/MCP, não na UI. Credenciais de teste são configuradas por nome; os valores ficam no cofre do runner.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
