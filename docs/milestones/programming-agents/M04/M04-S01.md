# M04-S01 — Configurar capacidades e políticas na dashboard

**Status:** in_progress — política durável por bot, validação no serviço compartilhado (UI/MCP) e política efetiva cobertas por Playwright e unitário; faltam E2E da configuração de projeto (comandos, browser, repositórios), exibição de origem/necessidade de reinício e E2E salvando dois bots; aceite do operador pendente.

**Milestone:** [M04 — Operação pela dashboard, Telegram e MCP](MILESTONE.md)
**Dependências:** [M00-S02](../M00/M00-S02.md), [M03-S01](../M03/M03-S01.md)
**Requisitos:** [R01](../REQUIREMENTS.md#r01), [R02](../REQUIREMENTS.md#r02), [R04](../REQUIREMENTS.md#r04), [R11](../REQUIREMENTS.md#r11), [R14](../REQUIREMENTS.md#r14)

## Resultado esperado

Configurar capacidades e políticas na dashboard. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

apps/dashboard bot/project settings; shared configuration service. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Criar configuração reutilizável de programação, retomada, autonomia e modelos por bot.
- [ ] Configurar comandos por pacote, browser e repositórios por projeto.
- [ ] Mostrar política efetiva, origem e necessidade de reinício quando aplicável.
- [x] Validar schema no serviço compartilhado usado por UI e MCP.

## Critérios de aceite

- [x] Criar segundo bot exige configuração, sem copiar implementação.
- [x] Permissões e capacidades são visíveis antes de iniciar trabalho.
- [x] Não há campo de teto financeiro aplicado por padrão ou implícito.
- [x] Salvar configuração inválida não altera parcialmente o bot.

## Validação e evidências exigidas

- [ ] Testar E2E criação/configuração de dois bots e round-trip via serviço.
- [x] Testar alterações concorrentes e resumo de políticas efetivas.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

bot_configuration_changed, project_configuration_changed; diff redigido e ator.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `6c77375` (telas e rota de política), `140cb12` (modelos e validação antes de ativar), `c337590` (schema). Caminhos: `apps/dashboard/src/features/bots/programming-settings.tsx`, `apps/dashboard/src/features/projects/programming-settings.tsx`, `apps/dashboard/src/app/api/programming/policy/route.ts`, `packages/bots/src/programming/models.ts`, `packages/bots/src/store.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário e Playwright.
- Evidências automatizadas: `apps/dashboard/tests/e2e/programming.spec.ts`: "configures a durable programming policy per bot and shows the effective policy", "validates the run model policy before activation and keeps it across reloads", "refuses anonymous access to runs and evidence"; `apps/dashboard/tests/e2e/bots.spec.ts`: "requires login and rejects cross-origin bot mutations"; `packages/bots/tests/models.test.ts`: "accepts different models per bot and rejects what cannot work", "refuses to save an invalid run policy, keeping the previous revision"; `packages/mcps/oinko/tests/bots.test.ts`: "rejects stale revisions, missing bots, unknown fields and invalid merged definitions without writing"; `packages/mcps/oinko/tests/runs.test.ts`: "updates the durable programming policy with a revision and records the capability change"; `packages/agent-runtime/tests/programming/policy.test.ts`: "defaults to disabled: migrating a bot never widens permissions and has no money cap"; `packages/bots/tests/pilot.test.ts`: "runs both in parallel with isolated queues, visibility and telemetry, and denies publication to the non-publisher". Comandos: `pnpm exec playwright test` (em `apps/dashboard`; `programming.spec.ts` 11/11); `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote).
- Evidências reais / aceite de operador: Pendente — aceite humano do operador na dashboard não registrado.
- Limitações e riscos residuais: Na validação `be30ad6`, o E2E de workspaces falhou por rótulo duplicado do switch de publicador da tela de projeto (corrigido em `835dcb5`); a tela de projeto não tem teste próprio. "Reinicie o bot para aplicar" é texto fixo, sem detecção de reinício necessário. Alterações concorrentes foram testadas no store/MCP (revisão esperada), não na UI.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
