# M04-S03 — Operar tarefas pelo Telegram e CLI

**Status:** in_progress — confirmação imediata com identificador, dedupe de update, 429 como erro de entrega e controle restrito à conversa testados; faltam /pause e /resume por canal com seleção por ambiguidade, contenção de spam, falha de notificação sem afetar o run, retries do adaptador e a jornada real no Telegram.

**Milestone:** [M04 — Operação pela dashboard, Telegram e MCP](MILESTONE.md)
**Dependências:** [M03-S05](../M03/M03-S05.md), [M03-S06](../M03/M03-S06.md)
**Requisitos:** [R01](../REQUIREMENTS.md#r01), [R11](../REQUIREMENTS.md#r11)

## Resultado esperado

Operar tarefas pelo Telegram e CLI. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

packages/channels/telegram; packages/channels/cli; runtime adapters. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Enviar confirmação breve com identificador depois de persistir o pedido.
- [ ] Adicionar /status, /pause, /resume e /cancel com seleção quando houver ambiguidade.
- [ ] Publicar progresso por mudança relevante e resumo final com evidências; conter spam.
- [ ] Reutilizar typing e cancelamento existentes; separar erro de entrega do erro de execução.

## Critérios de aceite

- [x] Telegram não fica esperando resposta final para reconhecer tarefa longa.
- [ ] Queda no envio de atualização não cancela o run nem repete o trabalho.
- [x] Comando no chat errado não controla tarefa de outra conversa sem autorização.
- [x] Mesmo contrato funciona por CLI, sem lógica de programação no adaptador.

## Validação e evidências exigidas

- [ ] Testar adaptadores com retries, duplicação de update e rate limit.
- [ ] Validar jornada real no Telegram com bot piloto e depois segundo bot.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

channel_request_received, progress_notification_sent, notification_failed, control_requested.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `bd3ee7a` (`ChannelCommands`, notifier e adaptador Telegram), `6eb8aac` (testes do adaptador para trabalhos). Caminhos: `packages/agent-runtime/src/programming/{channel,notifier}.ts`, `packages/channels/telegram/src/index.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário, simulado e Docker real (canal de controle do worker). O `packages/channels/telegram/tests/programming.test.ts` entrou no `pnpm test` da revisão `60fe1f9` (21 testes no pacote).
- Evidências automatizadas: `packages/channels/telegram/tests/programming.test.ts`: "answers run commands without waiting for the LLM and keys requests by update so a redelivery is deduplicated", "delivers progress only through the connection that owns the conversation and surfaces rate limits"; `packages/bots/tests/programming-run.test.ts`: "controls work from a channel without waiting for the agent and notifies the conversation"; `packages/agent-runtime/tests/programming/service.test.ts`: "cancels a queued run at once and controls from another conversation are refused"; `packages/mcps/oinko/tests/runs.test.ts`: "gives the same result for an equivalent operation from MCP, a channel and the dashboard service"; Docker real `packages/bots/tests/programming.e2e.test.ts`: "accepts over the channel, survives a disconnect and fixes the bug in the Docker sandbox". Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/programming.e2e.test.ts tests/evaluation.e2e.test.ts` (em `packages/bots`).
- Evidências reais / aceite de operador: Pendente — jornada real no Telegram com o bot piloto e depois com o segundo bot (sem bot e conversa reais nesta sessão).
- Limitações e riscos residuais: O canal "cli" é exercitado via `AgentRuntime`; o adaptador CLI não tem teste de trabalho próprio. A política de notificação (`notifications.minIntervalSeconds`) e o evento `notification_failed` não têm teste.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
