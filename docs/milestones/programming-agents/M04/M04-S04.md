# M04-S04 — Expor controle e consulta no MCP Oinko

**Status:** in_progress — iniciar sem bloquear, listar, consultar, controlar e explicar runs pelo MCP com escopo por bot, equivalência de ferramentas e round-trip de política testados; a consulta de evidências (`oinko_artifact`) não tem teste.

**Milestone:** [M04 — Operação pela dashboard, Telegram e MCP](MILESTONE.md)
**Dependências:** [M03-S05](../M03/M03-S05.md), [M03-S06](../M03/M03-S06.md), [M04-S01](../M04/M04-S01.md), [M01-S05](../M01/M01-S05.md)
**Requisitos:** [R01](../REQUIREMENTS.md#r01), [R02](../REQUIREMENTS.md#r02), [R11](../REQUIREMENTS.md#r11)

## Resultado esperado

Expor controle e consulta no MCP Oinko. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

packages/mcps/oinko; shared services; tool registry. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [ ] Expor iniciar/listar/consultar/controlar runs e consultar evidências com schemas paginados.
- [x] Reutilizar update de bots para novas capacidades e configurar projeto com revisão esperada.
- [x] Usar mesmos serviços, autorização e idempotência de dashboard/runtime.
- [x] Detectar equivalência de ferramentas internas e MCP para não carregar duas cópias no modelo.

## Critérios de aceite

- [x] MCP administra qualquer bot autorizado e nunca assume bot dev.
- [x] Resposta de iniciar informa runId, status e meio de acompanhar, sem bloquear até terminar.
- [x] MCP conectado a bot não herda privilégios administrativos da instalação.
- [x] Ferramentas selecionadas pelo Jev continuam sujeitas à autorização no executor.

## Validação e evidências exigidas

- [x] Testar contrato MCP real, round-trip de configuração e permissão negada.
- [x] Comparar resultados de operação equivalente via UI, canal e MCP.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

mcp_call_started, mcp_call_finished com runId e operationId; deduplicação de spans.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `eceb954` (ferramentas de runs no MCP e escopo `--bot`), `140cb12` (política de modelos pelo `oinko_update_bot`). Caminhos: `packages/mcps/oinko/src/{runs,bots,index,cli}.ts`, `packages/bots/src/programming/equivalence.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário (cliente do SDK MCP em transporte em memória).
- Evidências automatizadas: `packages/mcps/oinko/tests/runs.test.ts`: "registers exactly the tools the bots equivalence map knows about", "starts without waiting, lists, controls and explains runs through the shared service", "acts only as its bot when scoped, without administrative tools or other bots’ runs", "gives the same result for an equivalent operation from MCP, a channel and the dashboard service", "updates the durable programming policy with a revision and records the capability change"; `packages/mcps/oinko/tests/server.test.ts`: "exposes MCP tools, a guide and a prompt with a real protocol client", "rejects conflicting project identities, ambiguous reuse and changed refs without writes"; `packages/mcps/oinko/tests/bots.test.ts`: "rejects stale revisions, missing bots, unknown fields and invalid merged definitions without writing"; `packages/bots/tests/equivalence.test.ts`: "detects the Oinko MCP and scopes it to the bot without duplicating internal tools"; `packages/agent-runtime/tests/programming/store.test.ts`: "denies operations after revocation and records the denial"; `packages/oinko/tests/unit/core/tool-selection.test.ts`: "can select no operational tools for a greeting, without trusting invented tool ids". Comando: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote).
- Evidências reais / aceite de operador: Não aplicável para aceite; uso por cliente MCP externo (stdio) não registrado.
- Limitações e riscos residuais: O cliente dos testes é o SDK oficial em transporte em memória; stdio com cliente externo não foi exercitado nesta story. `oinko_artifact` sem teste.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
