# M00-S03 — Congelar cenários e medir baseline

**Status:** in_progress — cenários congelados e linhas de base simulada e Docker medidas; a baseline com provedor real (3 repetições por caso) não foi executada e a linha registrada não inclui intervenção humana nem consumo separado de Jev/resumo.

**Milestone:** [M00 — Contratos compartilhados e linha de base](MILESTONE.md)
**Dependências:** [M00-S01](../M00/M00-S01.md), [M00-S02](../M00/M00-S02.md)
**Requisitos:** [R15](../REQUIREMENTS.md#r15), [R16](../REQUIREMENTS.md#r16)

## Resultado esperado

Congelar cenários e medir baseline. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

fixtures de avaliação; docs/dashboard/CONTEXT-EVALUATION.md; testes existentes. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Versionar quatro casos reais: bug com teste, funcionalidade de monorepo, alteração visual e trabalho longo interrompido.
- [x] Fixar revisões dos repositórios, entradas, critérios de sucesso e manifestos de execução.
- [x] Medir estado atual, incluindo falhas e recursos ainda ausentes; separar fornecedor real, simulação e Docker.
- [ ] Registrar qualidade, tempo, intervenção, chamadas e consumo total incluindo Jev e resumo.

## Critérios de aceite

- [x] Cada caso tem critérios verificáveis antes da execução e artefatos esperados.
- [x] Baseline informa modelos, configuração, commit, data, amostra e custos indisponíveis.
- [x] Os números da avaliação antiga de contexto são referências históricas, não baseline de programação.
- [ ] Testes reais com provedor têm três repetições por caso quando a comparação final ocorrer.

## Validação e evidências exigidas

- [x] Executar harness de baseline em fixture descartável e conferir reprodutibilidade dos insumos.
- [x] Conferir que resultado ausente ou skipped não vira sucesso nem custo zero.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

evaluation_started, evaluation_case_finished; snapshot do dataset e da política.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `e7c4547` (dataset e harness isolado), `364fb42` (harness pelo runner no Docker), `d317b78` (CLI `evaluate`), `6eb8aac` (BASELINE medida). Caminhos: `packages/bots/evaluation/programming-baseline.v1.json`, `packages/bots/src/programming/evaluation/`, `packages/bots/src/evaluate.ts`, `docs/milestones/programming-agents/BASELINE.md`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: simulado (linha de base medida em `e7c4547`) e Docker real (harness pelo runner).
- Evidências automatizadas: `packages/bots/tests/evaluation.test.ts`: "runs the four frozen cases in isolated roots and records every case", "reproduces inputs exactly and never counts a skipped case as passed", "refuses to evaluate into the installation root and never publishes from an attempt"; Docker real `packages/bots/tests/evaluation.e2e.test.ts`: "runs frozen cases through the real runner and skips what needs a real preview"; `packages/agent-runtime/tests/programming/evaluation.test.ts`: "never turns unknown cost into zero", "versions datasets by content and never stores a changed dataset under the same version". Resultados: simulado 4/4 (4180 tokens, custo desconhecido), Docker 3 aprovados e `visual-selo` não executado, provedor real 12 tentativas skipped (`real_provider_unavailable`). Comandos: `OINKO_ROOT=<raiz> pnpm --filter @oinko/bots evaluate --bot <id> --dataset packages/bots/evaluation/programming-baseline.v1.json [--environment simulated|docker|real]`; `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/programming.e2e.test.ts tests/evaluation.e2e.test.ts` (em `packages/bots`).
- Evidências reais / aceite de operador: Pendente — baseline com provedor real (3 repetições por caso) não executada por falta de credencial de avaliação; `visual-selo` não executado no Docker por falta de ambiente de prévia real no harness.
- Limitações e riscos residuais: A linha simulada mede harness e plataforma, não a qualidade de um modelo (o "modelo" é o roteiro do caso). A tabela de BASELINE não registra intervenção humana (a métrica existe no harness) nem separa Jev/resumo, ausentes na simulação. Os quatro casos são fixtures sintéticas versionadas, não repositórios de produção.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
