# M00-S04 — Planejar migração e evolução compatível

**Status:** in_progress — migrações aditivas, backup/restauração e compatibilidade de schema testados; faltam teste explícito de conversa tradicional com a capacidade desativada, teste de leitura/drenagem de runs ao desativar e registro da revisão de branch/PR por entrega.

**Milestone:** [M00 — Contratos compartilhados e linha de base](MILESTONE.md)
**Dependências:** [M00-S01](../M00/M00-S01.md), [M00-S02](../M00/M00-S02.md)
**Requisitos:** [R03](../REQUIREMENTS.md#r03), [R17](../REQUIREMENTS.md#r17)

## Resultado esperado

Planejar migração e evolução compatível. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

stores SQLite; agent-runtime/service; documentação de configuração. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Desenhar migrações aditivas, versionamento e backup/restauração testável de SQLite.
- [x] Preservar conversas, bots, checkpoints e configurações atuais.
- [x] Definir ativação gradual por capacidade e compatibilidade da API de mensagem existente.
- [ ] Revisar estado de branches/PRs antes de cada entrega; PRs 5 e 6 estão merged na consulta de 23/09/2026.

## Critérios de aceite

- [x] Banco anterior é legível após migração, sem apagar histórico.
- [ ] Desativar capacidade impede novos runs e mantém leitura de resultados; runs ativos são drenados ou pausados explicitamente.
- [x] Rollback de binário só ocorre se schema for compatível; caso contrário restaura backup com parada coordenada.
- [x] Migração não depende de integrar automaticamente o redesign em andamento da dashboard.

## Validação e evidências exigidas

- [x] Testar cópia de banco antigo, upgrade, reabertura e recuperação de falha no meio da migração.
- [ ] Testar conversação tradicional com programação desativada.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

migration_started, migration_finished, capability_changed; falhas visíveis ao operador.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `c337590` (migrações aditivas com backup, restauração verificada, compatibilidade de binário; migração v3 da telemetria do SDK). Caminhos: `packages/agent-runtime/src/programming/store/{database,migrations}.ts`, `packages/oinko/src/telemetry/migrations.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário.
- Evidências automatizadas: `packages/agent-runtime/tests/programming/database.test.ts`: "applies every migration once, in order, and records versions", "keeps old data readable after an additive upgrade and backs up before migrating", "rolls back a migration that fails midway and stays readable at the previous version", "lets an older binary open a newer schema only when the unknown migrations are additive", "restores a verified backup over the current file with a coordinated stop"; `packages/oinko/tests/unit/telemetry/telemetry-database.test.ts`: "upgrades a v2 database to host events and correlation, keeping old executions readable"; `packages/oinko/tests/e2e/10-background-summary.test.ts`: "prepares an old conversation when the policy is turned on later"; `packages/agent-runtime/tests/programming/policy.test.ts`: "defaults to disabled: migrating a bot never widens permissions and has no money cap"; `packages/mcps/oinko/tests/runs.test.ts`: "updates the durable programming policy with a revision and records the capability change" (desativar recusa novo run). Comando: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote).
- Evidências reais / aceite de operador: Não aplicável para aceite. Pendente o registro da revisão do estado de branch/PR antes de cada entrega (há apenas commits locais, sem PR; a consulta aos PRs 5 e 6 está registrada em BASELINE.md de 23/09/2026).
- Limitações e riscos residuais: As suítes de chat existentes passam sem a capacidade, mas nenhum teste afirma o cenário "programação desativada". Desativar impede novos runs (testado); a leitura posterior de resultados e a drenagem ou pausa explícita de runs ativos não foram testadas. `restoreProgrammingBackup` existe, mas o RUNBOOK descreve só o backup, não a restauração nem o rollback de binário.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
