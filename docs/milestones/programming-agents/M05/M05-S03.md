# M05-S03 — Expor ações, leitura e diagnóstico de browser

**Status:** completed — navigate, snapshot, click, fill, wait, screenshot, refs stale, diagnóstico redigido de console/rede, timeout e submissão incerta testados em Docker real na revisão `18edc36`.

**Milestone:** [M05 — Browser isolado e validação funcional](MILESTONE.md)
**Dependências:** [M05-S02](../M05/M05-S02.md), [M04-S04](../M04/M04-S04.md)
**Requisitos:** [R11](../REQUIREMENTS.md#r11), [R12](../REQUIREMENTS.md#r12)

## Resultado esperado

Expor ações, leitura e diagnóstico de browser. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

browser tools; runner service; artifact storage. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Disponibilizar navigate, leitura estruturada, click, fill, wait e screenshot.
- [x] Capturar console e falhas de rede relevantes com filtros e limites de tamanho.
- [x] Restringir ações à sessão autorizada; identificar elementos por referência de snapshot e validar stale.
- [x] Retornar observação curta e referência a artefato completo.

## Critérios de aceite

- [x] Ferramenta não injeta HTML bruto ilimitado no contexto.
- [x] Elemento stale exige nova observação em vez de clicar em posição obsoleta.
- [x] Ações têm timeout e resultado rastreável, com incerteza quando submissão foi interrompida.
- [x] Rede e console ocultam cookies, Authorization e campos de formulário sensíveis.

## Validação e evidências exigidas

- [x] Testar formulário, navegação, erro HTTP, erro JS, timeout e layout mobile.
- [x] Testar captura full/none e artefatos extensos.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

browser_action_started, browser_action_finished, browser_console_error, browser_network_error, artifact_created.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `be40460` (ações, snapshot/refs, diagnóstico e redação), `4100d61` (ferramentas `browser_*` do bot e artefatos). Caminhos: `packages/environments/src/browser/{session,snapshot,redact}.ts`, `packages/bots/src/programming/delivery-tools.ts`.
- Revisão e ambiente testados: Revisão `18edc36`, relatório de validação em `validation-reports/` (`validation-18edc367c33a.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — runner/navegador 69 aprovados e 1 skipped (Railpack), worker+runner+harness 3/3 — e Playwright 46/46 aprovados; histórico em `validation-60fe1f978cf9.md` e `validation-be30ad61fc88.md`, este com 1 falha em `workspaces.spec.ts` corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário, simulado e Docker real.
- Evidências automatizadas: Docker real `packages/environments/tests/browser.e2e.test.ts`: "fills and submits a form, surfaces the intentional error and redacts console and network captures", "requires a fresh snapshot for stale refs and never acts on old positions", "captures mobile and full-page screenshots and full content as bounded artifacts", "reports an interrupted submit as uncertain instead of failed or done", "denies private, metadata, loopback and redirect targets, including subresources"; `browser-refs.test.ts`: "requires a snapshot and resolves refs of the current one only", "treats refs of an older snapshot, a navigation or another page as stale", "checks the live element: detached or changed role/name means stale, never old coordinates"; `browser-redact.test.ts`: "removes cookies, Authorization, Set-Cookie, bearer/basic tokens, JWTs and sensitive key=value pairs", "bounds text with an explicit truncation marker"; `browser-extension.test.ts`: "requires exactly one value source for fill and one condition for wait", "bounds identifiers, refs, timeouts and viewports", "binds sessions to a bot and a run and refuses callers without them, before starting a browser"; `packages/bots/tests/delivery.test.ts`: "fixes, validates the flow on the preview of the current revision and delivers a draft PR with CI" (eventos `browser_action_*` e `browser_network_error`); `packages/agent-runtime/tests/programming/artifacts.test.ts`: "keeps metadata but no content with capture none and shows it as unavailable". Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/docker.test.ts tests/runner.e2e.test.ts tests/failures.e2e.test.ts tests/browser.e2e.test.ts tests/workspace.e2e.test.ts tests/publication-docker.e2e.test.ts` (em `packages/environments`).
- Evidências reais / aceite de operador: Docker real executado; não depende de sistema externo nem de aceite.
- Limitações e riscos residuais: A captura `none`/`full` dos artefatos do navegador passa pelo mesmo `ArtifactStore.put` testado com artefato genérico, sem teste com captura de tela. Screenshots não redigem pixels (RUNBOOK §13).
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
