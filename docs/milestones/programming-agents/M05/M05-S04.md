# M05-S04 — Vincular preview e verificação funcional à revisão

**Status:** in_progress — prévia ligada à revisão, orquestração editar→check→prévia→verificação e invalidação por nova revisão testadas com runner simulado; mudança visual real não executada (harness sem prévia real), resumo e link pelo Telegram real pendentes e mudança de configuração só invalida na próxima prévia.

**Milestone:** [M05 — Browser isolado e validação funcional](MILESTONE.md)
**Dependências:** [M05-S03](../M05/M05-S03.md), [M02-S05](../M02/M02-S05.md), [M03-S06](../M03/M03-S06.md)
**Requisitos:** [R10](../REQUIREMENTS.md#r10), [R12](../REQUIREMENTS.md#r12), [R15](../REQUIREMENTS.md#r15)

## Resultado esperado

Vincular preview e verificação funcional à revisão. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

preview service; validation orchestrator; dashboard evidence. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Associar preview à worktree/commit ou hash de árvore e registrar configuração do ambiente.
- [x] Orquestrar reproduzir problema → editar → CLI → preview → validar fluxo.
- [x] Coletar screenshot, passos, console/rede e resultado por critério.
- [ ] Invalidar evidência se código ou configuração relevante mudar.

## Critérios de aceite

- [x] Preview healthy não é confundido com fluxo funcional aprovado.
- [x] Teste visual/funcional indica URL, revisão, viewport e pré-condições.
- [ ] Falha de build, healthcheck ou browser fica ligada ao passo correto.
- [ ] Dashboard exibe artefatos e Telegram entrega resumo e link autorizado.

## Validação e evidências exigidas

- [ ] Executar mudança visual real e formulário com erro intencional.
- [x] Rebuild em outra revisão e garantir que evidência antiga não aprove a nova.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

preview_started, preview_ready, preview_failed, functional_check_finished, evidence_invalidated.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `4100d61` (prévia, navegador e verificação funcional no run), `62b65fb` (relatório de entrega e painel). Caminhos: `packages/bots/src/programming/delivery-tools.ts`, `packages/agent-runtime/src/programming/{evidence,delivery-report}.ts`, `apps/dashboard/src/features/programming/run-detail.tsx`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário, simulado (runner simulado com prévia/navegador) e Playwright; formulário com erro intencional em Docker real (navegador no runner).
- Evidências automatizadas: `packages/agent-runtime/tests/programming/criteria.test.ts`: "approves only the exact revisions the preview was built from", "never confuses a healthy preview with an approved flow: the latest current result wins", "turns a functional check into a criterion so a failed flow blocks completion until it passes"; `packages/bots/tests/delivery.test.ts`: "fixes, validates the flow on the preview of the current revision and delivers a draft PR with CI", "does not let an old functional result approve a later revision", "denies the browser when the project disables it and persists the denial"; `packages/bots/tests/programming-run.test.ts`: "fixes a bug proven by a failing test and completes only on evidence of the final revision" (reprodução pelo teste antes de editar); `apps/dashboard/tests/e2e/programming.spec.ts`: "shows a draft PR with its CI state, the functional check revision and the screenshot"; Docker real `packages/environments/tests/browser.e2e.test.ts`: "fills and submits a form, surfaces the intentional error and redacts console and network captures"; Docker real `packages/bots/tests/evaluation.e2e.test.ts`: `visual-selo` skipped (`preview_environment_unavailable`). Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `pnpm exec playwright test` (em `apps/dashboard`; `programming.spec.ts` 11/11); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/docker.test.ts tests/runner.e2e.test.ts tests/failures.e2e.test.ts tests/browser.e2e.test.ts tests/workspace.e2e.test.ts tests/publication-docker.e2e.test.ts` (em `packages/environments`); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/programming.e2e.test.ts tests/evaluation.e2e.test.ts` (em `packages/bots`).
- Evidências reais / aceite de operador: Pendente — mudança visual real em prévia real e entrega do resumo com link pelo Telegram real.
- Limitações e riscos residuais: RUNBOOK §13: mudança na configuração do ambiente após a verificação só é detectada na próxima prévia/verificação; o harness não provisiona prévia real. Falha de build, healthcheck ou navegador ligada ao passo do run não tem teste dedicado.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
