# M07-S02 — Selecionar contexto e ferramentas progressivamente

**Status:** in_progress — catálogo compacto para o Jev, expansão por ToolSearch, recuperação limitada, ferramentas essenciais sempre disponíveis e instruções fixadas testados; faltam políticas de contexto separadas para principal e fast e teste de acesso histórico entre bots.

**Milestone:** [M07 — Contexto eficiente e fallback de modelo](MILESTONE.md)
**Dependências:** [M07-S01](../M07/M07-S01.md), [M02-S04](../M02/M02-S04.md), [M04-S04](../M04/M04-S04.md)
**Requisitos:** [R09](../REQUIREMENTS.md#r09), [R14](../REQUIREMENTS.md#r14)

## Resultado esperado

Selecionar contexto e ferramentas progressivamente. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

SDK tool selection; Jev; bounded history/tool-result retrieval. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Dar ao Jev catálogo compacto de capacidades e descrições sem carregar schemas completos de tudo a cada decisão.
- [x] Carregar schemas escolhidos sob orçamento e permitir expansão quando surgir necessidade.
- [x] Recuperar histórico e resultados por busca/range/referência limitada com permissões.
- [x] Fixar instruções do projeto, objetivo atual e controles essenciais mesmo ao reduzir contexto.

## Critérios de aceite

- [x] Escolha de ferramenta é sugestão, não autorização para executá-la.
- [x] Mudança de necessidade pode expandir ferramentas sem reiniciar tarefa.
- [ ] Principal e fast respeitam políticas próprias de contexto.
- [x] Economia inclui custo/tokens do Jev e dos resumos; não promete redução numérica antes de medir.

## Validação e evidências exigidas

- [x] Testar tarefa que exige ferramenta não escolhida inicialmente e fallback da seleção.
- [ ] Testar instruções essenciais preservadas, resultado longo e acesso histórico entre bots.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

routing_decision, tools_selected, tools_expanded, context_assembled, history_retrieved; tokens por componente.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `140cb12` (seleção progressiva, `alwaysAvailable`, ContextAssembly no `agent_start`). Caminhos: `packages/oinko/src/tools/tool-selection.ts`, `packages/oinko/src/agent.ts`, `packages/agent-runtime/src/programming/agent-executor.ts`, `packages/bots/src/runner.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário e e2e do SDK com decisor/provedor simulados; simulado no runtime.
- Evidências automatizadas: `packages/oinko/tests/unit/core/tool-selection.test.ts`: "batches routing and tool questions, uses task state, and selects more than one tool", "keeps all authorized tools available on decision outage", "can select no operational tools for a greeting, without trusting invented tool ids", "discovers tools during a turn without changing another turn or the shared registry", "archives complete output before truncation and scopes exact retrieval", "keeps tools marked alwaysAvailable exposed even when none was selected", "does not ask the decider about essential tools"; `packages/oinko/tests/e2e/09-adaptive-context.test.ts`: "discovers a missing tool and refreshes the actual model schemas before executing it", "keeps full tools when the selector is down and exposes the fallback", "reports the exposed tools, whether they were selected and tokens per component"; `packages/agent-runtime/tests/programming/agent-executor.test.ts`: "journals selected tools, context composition, expansions and history retrievals", "keeps project instructions pinned in later cycle prompts", "marks the run control tools as always available under tool selection"; `usage.test.ts`: "collects main, fast, summary and Jev calls of a run from the SDK telemetry database"; `store.test.ts`: "denies operations after revocation and records the denial"; `packages/bots/tests/delivery.test.ts`: "offers no browser or publication tools to a bot without those capabilities". Comando: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote).
- Evidências reais / aceite de operador: Pendente — seleção com Jev e provedor reais não medida; nenhuma economia numérica é afirmada.
- Limitações e riscos residuais: O isolamento de recuperação foi testado entre threads, não entre bots. Não há política de contexto distinta para o modelo rápido nos runs.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
