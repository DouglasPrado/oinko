# M07-S03 — Aplicar fallback por indisponibilidade e latência

**Status:** in_progress — troca fast→principal por latência (sem saída útil) e por indisponibilidade, uma troca por chamada, heartbeats ignorados, texto parcial preservado, tool não repetida e resposta tardia descartada testados com provedor simulado; provedor real pendente e sem verificação negativa de permissão pertinente.

**Milestone:** [M07 — Contexto eficiente e fallback de modelo](MILESTONE.md)
**Dependências:** [M07-S02](../M07/M07-S02.md), [M01-S04](../M01/M01-S04.md)
**Requisitos:** [R14](../REQUIREMENTS.md#r14)

## Resultado esperado

Aplicar fallback por indisponibilidade e latência. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

SDK provider calls; runtime model policy; streaming delivery. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Trocar fast para principal após 15 segundos sem saída útil ou indisponibilidade recuperável do provedor.
- [x] Permitir no máximo uma troca automática por chamada lógica e cancelar tentativa anterior.
- [x] Definir saída útil como conteúdo consumível ou chamada de ferramenta válida; manter heartbeats separados.
- [x] Reconciliar resposta parcial e execução de tool antes de continuar; preservar conteúdo entregue.

## Critérios de aceite

- [x] Ping/heartbeat não reinicia prazo de 15 segundos.
- [x] Texto parcial já entregue não é apagado silenciosamente nem duplicado.
- [x] Tool iniciada não é executada duas vezes pelo fallback.
- [x] Ambas tentativas constam em uso e latência; nenhuma seleção depende de teto financeiro.

## Validação e evidências exigidas

- [x] Testar ausência de resposta, 503, token vazio, texto parcial e tool parcial.
- [x] Testar resposta tardia da tentativa abortada e indisponibilidade dos dois modelos.
- [ ] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [ ] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

model_fallback_triggered, model_attempt_cancelled, model_attempt_finished; razão e IDs lógicos.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `3b61cf7` (fallback por latência em `packages/oinko/src/core/react-loop.ts`), `140cb12` (`fallbackAfterMs` na política do run e roteamento no runner), eventos `model_fallback_triggered`/`model_attempt_*` em `packages/agent-runtime/src/programming/agent-executor.ts`.
- Revisão e ambiente testados: Revisão `18edc36`, relatório de validação em `validation-reports/` (`validation-18edc367c33a.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — runner/navegador 69 aprovados e 1 skipped (Railpack), worker+runner+harness 3/3 — e Playwright 46/46 aprovados; histórico em `validation-60fe1f978cf9.md` e `validation-be30ad61fc88.md`, este com 1 falha em `workspaces.spec.ts` corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário (SDK) e simulado (runtime).
- Evidências automatizadas: `packages/oinko/tests/unit/core/latency-fallback.test.ts`: "switches once after the limit without useful output and records both attempts", "does not let heartbeats or empty deltas reset the limit", "keeps the fast model once it delivered useful output in time", "falls back on provider unavailability (503) as well", "keeps partial text and asks the main model to continue without duplicating it", "never runs a started tool twice when the attempt fails after requesting it", "ignores a late answer from the cancelled attempt", "switches only once: when both models are unavailable the error is reported"; `packages/agent-runtime/tests/programming/agent-executor.test.ts`: "journals routing, both attempts of a latency fallback and pending context preparation"; `policy.test.ts`: "defaults to disabled: migrating a bot never widens permissions and has no money cap" (`fallbackAfterMs` 15000, sem teto); `usage.test.ts`: "reconciles late usage and confirmed cost once, and counts aborted attempts that reported usage". Comando: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote).
- Evidências reais / aceite de operador: Pendente — provedor real lento/indisponível (VALIDATION.md: Modelos → provedor real).
- Limitações e riscos residuais: Os testes usam limite de 60 ms; os 15 s vêm do default da política. O fallback exige Jev e modelo rápido configurados. Não há verificação negativa de permissão pertinente citável para esta story, por isso os dois checkboxes comuns ficaram desmarcados.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
