# M07-S01 — Resumir histórico em segundo plano com consistência

**Status:** in_progress — resumo em segundo plano com checkpoint, descarte de resumo stale, histórico preservado, preparação de conversa antiga e aviso de preparação testados com provedor simulado; faltam testes de serialização concorrente, mensagem nova durante o resumo, retry/indisponibilidade, orçamento do fast, consumo de resumo com falha e comparação com tokens reais.

**Milestone:** [M07 — Contexto eficiente e fallback de modelo](MILESTONE.md)
**Dependências:** [M01-S04](../M01/M01-S04.md), [M03-S01](../M03/M03-S01.md)
**Requisitos:** [R14](../REQUIREMENTS.md#r14)

## Resultado esperado

Resumir histórico em segundo plano com consistência. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

SDK adaptive-context; conversation stores; host scheduling. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Reutilizar checkpoints existentes para resumo após crescimento de histórico.
- [ ] Serializar atualização por conversa e comparar revisão antes de gravar.
- [x] Descartar resumo stale, manter histórico completo e preparar conversas antigas ao ativar política.
- [x] Mostrar preparação inicial quando resumo ainda não estiver disponível.

## Critérios de aceite

- [ ] Fast não recebe automaticamente histórico inteiro; recebe orçamento e contexto pertinente.
- [x] Resumo não remove mensagem original nem perde pares de chamadas/resultados necessários.
- [ ] Falha de resumo mantém caminho seguro explícito e consumo registrado.
- [ ] Nova mensagem durante resumo impede sobreposição de resultado stale.

## Validação e evidências exigidas

- [ ] Testar corrida entre chat e resumo, histórico antigo, retry e provider indisponível.
- [ ] Comparar tokens reais e primeira resposta com mesma conversa de baseline.
- [ ] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [ ] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

summary_scheduled, summary_finished, summary_discarded, context_preparation_pending.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `3b61cf7` (resumo em segundo plano no SDK). Caminhos: `packages/oinko/src/agent.ts` (`prepareContext` e fila por conversa), `packages/oinko/src/core/working-context.ts`, `packages/bots/src/runner.ts` (`summaryMode: 'background'` nos runs), `packages/agent-runtime/src/programming/agent-executor.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário e e2e do SDK com provedor simulado.
- Evidências automatizadas: `packages/oinko/tests/e2e/10-background-summary.test.ts`: "answers at once with the recent window, then summarizes and uses the summary next turn", "discards a summary made stale by a reset during its execution", "keeps the safe path and reports a failed summary without losing the turn", "prepares an old conversation when the policy is turned on later", "keeps inline mode unchanged: the summary is made before answering"; `packages/oinko/tests/unit/core/working-context.test.ts`: "retains pinned tool pairs and does not promote archived output into instructions", "never splits a pending tool-call group or summarizes the current user message"; `packages/agent-runtime/tests/programming/agent-executor.test.ts`: "journals routing, both attempts of a latency fallback and pending context preparation". Comando: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote).
- Evidências reais / aceite de operador: Pendente — tokens reais e tempo até a primeira resposta com provedor real (sem credencial).
- Limitações e riscos residuais: O descarte por stale foi testado com reset do histórico, não com mensagem nova durante o resumo. O orçamento do fast (`fastInputTokens`) é preexistente e sem teste nesta entrega. Não há verificação negativa de permissão pertinente citável para esta story, por isso os dois checkboxes comuns ficaram desmarcados.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
