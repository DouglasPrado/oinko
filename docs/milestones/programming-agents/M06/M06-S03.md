# M06-S03 — Criar ou atualizar draft PR sem duplicação

**Status:** in_progress — identidade reaproveitável do PR, reconciliação após timeout, PR por repositório e mensagem final com validações reais testados com GitHub simulado; fluxo real com dois repositórios de teste pendente.

**Milestone:** [M06 — GitHub App e entrega automática em draft](MILESTONE.md)
**Dependências:** [M06-S02](../M06/M06-S02.md), [M03-S04](../M03/M03-S04.md)
**Requisitos:** [R05](../REQUIREMENTS.md#r05), [R08](../REQUIREMENTS.md#r08), [R13](../REQUIREMENTS.md#r13)

## Resultado esperado

Criar ou atualizar draft PR sem duplicação. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

GitHub API adapter; run publication records; MCP. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Definir identidade bot/projeto/task/repo e run originador para um PR reaproveitável por trabalho.
- [x] Publicar branch da tarefa e criar draft com problema, mudanças, testes, preview e pendências.
- [x] Consultar referência remota e PR existente após timeout antes de repetir efeito.
- [x] Vincular PRs separados quando tarefa envolver múltiplos repositórios.

## Critérios de aceite

- [x] Retry após criação remota com resposta perdida encontra o PR e não abre outro.
- [x] Nova execução na mesma tarefa atualiza PR aberto compatível; PR fechado/merged exige decisão registrada.
- [x] PR permanece draft e não é marcado como mergeado ou aprovado.
- [x] Mensagem final lista links e validações reais, incluindo skipped/pendências.

## Validação e evidências exigidas

- [x] Testar timeout antes/depois de push e de create PR.
- [ ] Executar fluxo real com dois repos de teste e garantir idempotência.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

pull_request_reconciled, draft_pull_request_created, draft_pull_request_updated, publication_blocked.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `013ab44` (draft PR e reconciliação), `4100d61` (publicação no run), `62b65fb` (relatório de entrega). Caminhos: `packages/environments/src/publication/service.ts`, `packages/agent-runtime/src/programming/{service,delivery-report}.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário (GitHub simulado), simulado e Playwright.
- Evidências automatizadas: `packages/environments/tests/publication-flow.test.ts`: "reviews, commits the exact revision, pushes the task branch and opens one draft PR", "reconciles a timeout before the push and after the push without a second push or PR", "recovers after a runner crash between push and receipt", "keeps one draft PR per task: updates the open one, blocks on closed, reconciles a lost create", "refuses a PR before the branch exists remotely", "publishes one draft PR per repository and links them"; `packages/agent-runtime/tests/programming/criteria.test.ts`: "keeps one draft publication per task repository across runs", "requires a draft for each repository the run changed", "never presents skipped, infrastructure or pending CI as success"; `contracts.test.ts`: "keeps Task as the unit of work and lets one run reference a PR per repository"; `packages/bots/tests/delivery.test.ts`: "fixes, validates the flow on the preview of the current revision and delivers a draft PR with CI"; `packages/environments/tests/publication-policy.test.ts`: "requires an allowed publisher bot for every publication command; admin only reads status and installation". Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `pnpm exec playwright test` (em `apps/dashboard`; `programming.spec.ts` 11/11).
- Evidências reais / aceite de operador: Pendente — fluxo real com dois repositórios de teste e GitHub App real (depende do operador).
- Limitações e riscos residuais: PR fechado ou merged bloqueia a atualização; a decisão sobre ele fica com o operador, fora do fluxo automático.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
