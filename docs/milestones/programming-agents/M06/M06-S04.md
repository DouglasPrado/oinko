# M06-S04 — Acompanhar CI e estados externos

**Status:** in_progress — CI pelo SHA exato, checks obrigatórios, 429, instalação revogada, ausência de checks e rótulo "não validado integralmente" testados com GitHub simulado; CI real do repositório de teste pendente.

**Milestone:** [M06 — GitHub App e entrega automática em draft](MILESTONE.md)
**Dependências:** [M06-S03](../M06/M06-S03.md), [M01-S05](../M01/M01-S05.md)
**Requisitos:** [R06](../REQUIREMENTS.md#r06), [R13](../REQUIREMENTS.md#r13), [R15](../REQUIREMENTS.md#r15)

## Resultado esperado

Acompanhar CI e estados externos. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

GitHub polling; runtime checks; dashboard/Telegram. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Consultar checks/status por polling enquanto trabalho estiver ativo, respeitando rate limits.
- [x] Associar resultado ao SHA exato e atualizar evidências do run.
- [x] Distinguir queued/running/passed/failed/cancelled/unknown e jobs obrigatórios.
- [x] Parar polling conforme término/política sem exigir webhook público na primeira versão.

## Critérios de aceite

- [x] CI de commit anterior não aprova o atual.
- [x] Ausência de checks não é sucesso inferido.
- [x] Instalação revogada ou rate limit gera estado explícito e retomável.
- [x] Draft publicado pode ser entregue com CI pendente, mas não rotulado validado integralmente.

## Validação e evidências exigidas

- [x] Testar troca de SHA durante polling, 429, CI cancelado e resultado ausente.
- [ ] Verificar CI real do repo de testes e navegação pelo link do check.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

ci_poll_finished, ci_check_updated, ci_status_unavailable.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `013ab44` (normalização e consulta de CI), `4100d61` (ferramenta `publication_ci`), `62b65fb` (CI no painel e no relatório). Caminhos: `packages/environments/src/publication/{checks,service}.ts`, `packages/bots/src/programming/delivery-tools.ts`.
- Revisão e ambiente testados: Revisão `18edc36`, relatório de validação em `validation-reports/` (`validation-18edc367c33a.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — runner/navegador 69 aprovados e 1 skipped (Railpack), worker+runner+harness 3/3 — e Playwright 46/46 aprovados; histórico em `validation-60fe1f978cf9.md` e `validation-be30ad61fc88.md`, este com 1 falha em `workspaces.spec.ts` corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário (GitHub simulado), simulado e Playwright.
- Evidências automatizadas: `packages/environments/tests/publication-ci.test.ts`: "never attributes an older commit result to the current head", "reports required checks when readable, unknown otherwise, and waits for missing ones", "turns rate limits and a revoked installation into explicit, resumable states"; `publication-policy.test.ts`: "maps check runs and statuses to one state vocabulary", "never infers success from absence, skipped-only or another commit", "uses the newest re-run and waits for missing required checks", "requires an allowed publisher bot for every publication command; admin only reads status and installation"; `packages/agent-runtime/tests/programming/criteria.test.ts`: "labels a draft as fully validated only when CI of its commit passed"; `packages/bots/tests/delivery.test.ts`: "fixes, validates the flow on the preview of the current revision and delivers a draft PR with CI" (evento `ci_poll_finished`); `apps/dashboard/tests/e2e/programming.spec.ts`: "shows a draft PR with its CI state, the functional check revision and the screenshot". Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `OINKO_DOCKER_TEST=1 pnpm exec playwright test` (em `apps/dashboard`; 46/46 na revisão `18edc36`, com `programming.spec.ts` e `programming-ops.spec.ts`).
- Evidências reais / aceite de operador: Pendente — CI real do repositório de testes e navegação pelo link do check.
- Limitações e riscos residuais: O polling acontece enquanto `publication_ci` aguarda (`waitSeconds`); sem webhook, mudanças posteriores do CI só aparecem em nova consulta.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
