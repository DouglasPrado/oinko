# M03-S02 — Despachar em segundo plano e ordenar fila por bot

**Status:** in_progress — aceite com runId imediato, fila durável por bot, dedupe e controle independente testados (inclusive desconexão no Docker real); faltam testes do reconhecimento pedido novo vs. esclarecimento pelas ferramentas de conversa e da preservação do chat legado.

**Milestone:** [M03 — Execução durável e controle do trabalho](MILESTONE.md)
**Dependências:** [M03-S01](../M03/M03-S01.md)
**Requisitos:** [R04](../REQUIREMENTS.md#r04), [R08](../REQUIREMENTS.md#r08), [R11](../REQUIREMENTS.md#r11)

## Resultado esperado

Despachar em segundo plano e ordenar fila por bot. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

agent-runtime/service e host; dispatch de canais. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Separar aceite do pedido e execução; devolver runId imediatamente após persistência.
- [x] Manter um run de programação ativo por bot e fila durável para novos trabalhos.
- [x] Enviar status/pause/resume/cancel por via de controle independente da fila.
- [ ] Reconhecer novo pedido versus esclarecimento do run ativo com contexto e confirmação quando ambíguo.

## Critérios de aceite

- [x] Fechar página ou conexão HTTP após aceite não aborta o trabalho durável.
- [x] Dois bots podem avançar separadamente; mesma worktree mantém exclusão entre escritores.
- [x] Pedido repetido com mesma chave não cria dois runs.
- [ ] Chat legado preserva comportamento e não passa involuntariamente a criar trabalhos.

## Validação e evidências exigidas

- [x] Testar aceite seguido de desconexão e reinício com fila pendente.
- [x] Testar dois bots, três pedidos no mesmo bot e status durante comando longo.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

run_queued, run_dispatched, request_deduplicated, queue_wait_measured.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `c337590` (serviço, fila e dedupe), `bd3ee7a` (worker, comandos de canal e `programmingChatTools`), `e3e429b` (e2e com worker real). Caminhos: `packages/agent-runtime/src/programming/{service,channel}.ts`, `packages/bots/src/runner.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário, simulado e Docker real.
- Evidências automatizadas: `packages/agent-runtime/tests/programming/service.test.ts`: "returns the runId right after persistence and executes later", "keeps one active run per bot while two bots advance in parallel", "does not create two runs for a repeated request key", "survives a restart with a pending queue: another process picks it up", "answers status and control while a long command is running", "refuses a bot starting work in another bot’s name or a project it cannot access", "requires confirmation for an incompatible objective and records the decision"; `store.test.ts`: "serializes writers on the same worktree across bots"; Docker real `packages/bots/tests/programming.e2e.test.ts`: "accepts over the channel, survives a disconnect and fixes the bug in the Docker sandbox". Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/programming.e2e.test.ts tests/evaluation.e2e.test.ts` (em `packages/bots`).
- Evidências reais / aceite de operador: Pendente — pedido real por Telegram (ver M04-S03).
- Limitações e riscos residuais: O reconhecimento de pedido novo vs. esclarecimento em conversa livre depende de `programmingChatTools`, sem teste; os comandos explícitos (`/tarefa`, `/orientar`) e a confirmação de mudança de objetivo são testados. Nenhum teste afirma que mensagens comuns com a capacidade ativa não criam trabalho.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
