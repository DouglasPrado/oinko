# M03-S05 — Pausar, retomar e cancelar com semântica segura

**Status:** in_progress — pausa em ponto seguro, cancelamento sem rollback, processo resistente com escalonamento e controles sem esperar o LLM testados; falta testar pausa durante patch e durante publicação.

**Milestone:** [M03 — Execução durável e controle do trabalho](MILESTONE.md)
**Dependências:** [M03-S02](../M03/M03-S02.md), [M03-S04](../M03/M03-S04.md)
**Requisitos:** [R04](../REQUIREMENTS.md#r04), [R08](../REQUIREMENTS.md#r08), [R11](../REQUIREMENTS.md#r11)

## Resultado esperado

Pausar, retomar e cancelar com semântica segura. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

controle do runtime; AbortSignal; processos do runner. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Persistir pedido de pausa e parar no próximo ponto seguro.
- [x] Cancelar processos controlados com encerramento gracioso e escalonamento, preservando arquivos e commits.
- [x] Retomar com novas permissões conferidas e operações pendentes reconciliadas.
- [x] Distinguir pedido de cancelamento, confirmação do processo e efeito externo incerto.

## Critérios de aceite

- [ ] Pausa durante publicação espera ponto seguro ou registra incerteza, sem declarar rollback.
- [x] Cancelamento nunca executa reset/clean/delete de worktree implicitamente.
- [x] Comandos de controle não aguardam conclusão de uma chamada LLM longa para serem aceitos.
- [x] Resposta informa o que parou e o que ainda exige reconciliação.

## Validação e evidências exigidas

- [ ] Testar pause durante LLM, teste, patch e publicação.
- [x] Testar processo filho resistente, repetição de cancel e resume de estado inválido.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

control_requested, run_paused, process_terminated, run_cancelled, operation_uncertain.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `c337590` (controles persistidos), `8a7faa6` (`stopJob` com TERM e escalonamento), `bd3ee7a` (comandos de canal), `6c77375` (controles na dashboard). Caminhos: `packages/agent-runtime/src/programming/{service,channel}.ts`, `packages/environments/src/workspace/extension.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário, simulado, Docker real e Playwright.
- Evidências automatizadas: `packages/agent-runtime/tests/programming/service.test.ts`: "answers status and control while a long command is running", "pauses during an LLM call immediately and resumes with permissions checked again", "cancels during a tool, reports the uncertain effect and never rolls back", "cancels a queued run at once and controls from another conversation are refused"; Docker real `packages/environments/tests/workspace.e2e.test.ts`: "times out, stops a TERM-resistant process group and never reports it as passed"; `packages/bots/tests/programming-run.test.ts`: "controls work from a channel without waiting for the agent and notifies the conversation"; `packages/channels/telegram/tests/programming.test.ts`: "answers run commands without waiting for the LLM and keys requests by update so a redelivery is deduplicated"; `apps/dashboard/tests/e2e/programming.spec.ts`: "distinguishes a requested pause from a paused run and keeps it across reloads". Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/docker.test.ts tests/runner.e2e.test.ts tests/failures.e2e.test.ts tests/browser.e2e.test.ts tests/workspace.e2e.test.ts tests/publication-docker.e2e.test.ts` (em `packages/environments`); `pnpm exec playwright test` (em `apps/dashboard`; `programming.spec.ts` 11/11).
- Evidências reais / aceite de operador: Pendente — controle por Telegram real não executado (ver M04-S03).
- Limitações e riscos residuais: Pausa durante patch e durante publicação usa o mesmo mecanismo de operação em andamento (espera o fim ou registra incerteza), sem teste dedicado.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
