# M02-S05 — Revisar diff e executar verificações pertinentes

**Status:** completed — snapshot/diff separando alterações do usuário, checks vinculados à revisão, verificação ampliada só com justificativa, classificação código vs. ambiente, saída extensa e invalidação testados (inclusive Docker real); não depende de sistema externo.

**Milestone:** [M02 — Entendimento do projeto e edição precisa](MILESTONE.md)
**Dependências:** [M02-S03](../M02/M02-S03.md), [M02-S04](../M02/M02-S04.md)
**Requisitos:** [R09](../REQUIREMENTS.md#r09), [R10](../REQUIREMENTS.md#r10), [R15](../REQUIREMENTS.md#r15)

## Resultado esperado

Revisar diff e executar verificações pertinentes. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

runner Git; checks service; context tools. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Capturar estado Git anterior e diff do run sem atribuir alterações do usuário ao bot.
- [x] Executar testes/lint/build escolhidos pelo pacote afetado e permitir verificação ampliada justificada.
- [x] Resumir saída com referências ao log completo e classificar falha de código vs ambiente.
- [x] Produzir evidências associadas ao commit ou hash da árvore ainda não commitada.

## Critérios de aceite

- [x] Pequena alteração não exige reescrever arquivo inteiro.
- [x] Check skipped/timeout/falha de infraestrutura não é reportado como aprovado.
- [x] Diff final relaciona arquivos tocados e mudanças preexistentes.
- [x] Teste só comprova a revisão realmente executada; nova edição invalida aprovação anterior.

## Validação e evidências exigidas

- [x] Realizar bug com teste em sandbox Docker e conferir diff/resultado.
- [x] Testar saída extensa, falha de instalação e alteração de arquivo depois do check.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

git_diff_captured, check_started, check_finished; comando, diretório, revisão e resultado.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `7a6a00d` (`gitSnapshot`/`gitDiff`), `8a7faa6` (checks destacados com revisão antes/depois), `bd3ee7a` (ferramentas `workspace_check`/`workspace_diff`), `c337590` (critérios e invalidação), `5a20c00` (`scope: 'repository'` exige `justification`). Caminhos: `packages/environments/src/workspace/{ops,extension}.ts`, `packages/bots/src/programming/run-tools.ts`, `packages/agent-runtime/src/programming/evidence.ts`.
- Revisão e ambiente testados: Revisão `9c3d44f`, relatório de validação em `validation-reports/` (`validation-9c3d44fb133d.md`: build, typecheck, lint, test, suítes por pacote, gates de IDs de piloto e de links internos, Docker real — runner/navegador 69 aprovados e 1 skipped (Railpack), worker+runner+harness 6/6, MCP Oinko 3/3 — e Playwright 48/48 aprovados; histórico em `validation-18edc367c33a.md`, `validation-60fe1f978cf9.md` e `validation-be30ad61fc88.md`, este com 1 falha em `workspaces.spec.ts` corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário, simulado e Docker real.
- Evidências automatizadas: `packages/environments/tests/workspace-ops.test.ts`: "separates the run’s changes from changes that existed before it started"; `packages/bots/tests/delivery.test.ts`: "widens a check beyond the affected package only with a justification and estimates read tokens"; `packages/agent-runtime/tests/programming/service.test.ts`: "never treats skipped, timeout or infrastructure checks as approval", "invalidates approval when the code changes after the check"; Docker real `packages/environments/tests/workspace.e2e.test.ts`: "fixes a bug proven by a failing test and binds the passing check to the exact revision", "classifies infrastructure failures, bounds long output and detects edits made during a check", "times out, stops a TERM-resistant process group and never reports it as passed"; Docker real `packages/bots/tests/programming.e2e.test.ts`: "accepts over the channel, survives a disconnect and fixes the bug in the Docker sandbox"; `packages/environments/tests/workspace-extension.test.ts`: "authorizes every operation by project and hides other bots’ journals". Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/docker.test.ts tests/runner.e2e.test.ts tests/failures.e2e.test.ts tests/browser.e2e.test.ts tests/workspace.e2e.test.ts tests/publication-docker.e2e.test.ts` (em `packages/environments`); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/programming.e2e.test.ts tests/evaluation.e2e.test.ts tests/programming.test.ts` (em `packages/bots`).
- Evidências reais / aceite de operador: Não aplicável — sem sistema externo nem aceite exigido.
- Limitações e riscos residuais: A verificação do repositório inteiro exige justificativa (`justification_required`) e roda na raiz; a escolha do check continua a cargo do modelo. A saída é resumida (`outputTail`) com log completo via `jobLogs`.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
