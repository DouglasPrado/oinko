# M02-S01 — Buscar caminhos e conteúdo na worktree

**Status:** completed — busca por nome/glob/conteúdo, filtros, paginação estável, desfecho de timeout distinto, symlink e escopo das ferramentas do run testados em unitário, simulado e Docker real; não depende de sistema externo.

**Milestone:** [M02 — Entendimento do projeto e edição precisa](MILESTONE.md)
**Dependências:** [M00-S02](../M00/M00-S02.md), [M01-S02](../M01/M01-S02.md)
**Requisitos:** [R02](../REQUIREMENTS.md#r02), [R09](../REQUIREMENTS.md#r09)

## Resultado esperado

Buscar caminhos e conteúdo na worktree. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

packages/environments; packages/bots/programming-tools; packages/mcps/oinko. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Criar operações compartilhadas de busca por nome, glob e conteúdo.
- [x] Filtrar por repositório/pacote, ignorar dependências, gerados e binários por padrão com override explícito.
- [x] Paginar resultados e devolver caminho, linha, trecho e motivo de truncamento.
- [x] Expor um catálogo sem duplicar ferramentas equivalentes workspace e MCP.

## Critérios de aceite

- [x] Consulta grande não despeja todo o repositório no modelo.
- [x] Dois bots só pesquisam raízes autorizadas e não seguem symlink externo.
- [x] Paginação e ordenação são estáveis para a mesma revisão de arquivos.
- [x] Ausência de resultado difere de erro, exclusão por filtro ou timeout.

## Validação e evidências exigidas

- [x] Testar monorepo, Unicode, arquivo binário, ignore e milhares de ocorrências.
- [x] Testar path traversal, symlink e autorização pelo MCP e ferramentas internas.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

workspace_search; registrar filtros, contagens, bytes, duração e truncamento.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `7a6a00d` (operações de workspace e extensão do runner), `bd3ee7a` (ferramenta `workspace_search`), `eceb954` (mapa de equivalência MCP), `7562987` (testes de timeout e de escopo das ferramentas do run). Caminhos: `packages/environments/src/workspace/{ops,extension}.ts`, `packages/bots/src/programming/{run-tools,equivalence}.ts`.
- Revisão e ambiente testados: Revisão `9c3d44f`, relatório de validação em `validation-reports/` (`validation-9c3d44fb133d.md`: build, typecheck, lint, test, suítes por pacote, gates de IDs de piloto e de links internos, Docker real — runner/navegador 69 aprovados e 1 skipped (Railpack), worker+runner+harness 6/6, MCP Oinko 3/3 — e Playwright 48/48 aprovados; histórico em `validation-18edc367c33a.md`, `validation-60fe1f978cf9.md` e `validation-be30ad61fc88.md`, este com 1 falha em `workspaces.spec.ts` corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário, simulado e Docker real.
- Evidências automatizadas: `packages/environments/tests/workspace-ops.test.ts`: "finds paths and content in a monorepo, skipping ignored, generated and binary files by default", "filters by package and glob and handles Unicode paths and content", "distinguishes no result, exclusion by filter and pages thousands of hits stably", “reports a search cut by its time budget as timeout, never as "no matches"”, "never follows a symlink out of the worktree"; `workspace-extension.test.ts`: "authorizes every operation by project and hides other bots’ journals"; `packages/bots/tests/delivery.test.ts`: "refuses a repository outside the run and works only after the task exists"; `packages/bots/tests/equivalence.test.ts`: "detects the Oinko MCP and scopes it to the bot without duplicating internal tools"; `packages/mcps/oinko/tests/runs.test.ts`: "registers exactly the tools the bots equivalence map knows about"; `packages/mcps/oinko/tests/server.test.ts`: "validates URLs and paths, and reports command errors rather than success" (leitura com `../secret` recusada); Docker real `packages/environments/tests/workspace.e2e.test.ts`: "searches, reads by range, patches with preconditions and diffs inside the container". Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/docker.test.ts tests/runner.e2e.test.ts tests/failures.e2e.test.ts tests/browser.e2e.test.ts tests/workspace.e2e.test.ts tests/publication-docker.e2e.test.ts` (em `packages/environments`).
- Evidências reais / aceite de operador: Não aplicável — sem sistema externo nem aceite exigido.
- Limitações e riscos residuais: O MCP Oinko não expõe busca; a leitura por MCP recusa traversal e, com `--bot`, usa o cliente do runner com o bot do escopo, autorizado por projeto no runner. Não há teste ponta a ponta de leitura por MCP com escopo de bot contra outro projeto.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
