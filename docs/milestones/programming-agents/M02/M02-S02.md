# M02-S02 — Ler intervalos com versão de conteúdo

**Status:** in_progress — leitura por intervalo com hash, limites, continuação, Unicode/CRLF e erros explícitos testados (inclusive no Docker real); falta registrar tokens de entrada na comparação da paginação.

**Milestone:** [M02 — Entendimento do projeto e edição precisa](MILESTONE.md)
**Dependências:** [M02-S01](../M02/M02-S01.md)
**Requisitos:** [R09](../REQUIREMENTS.md#r09), [R14](../REQUIREMENTS.md#r14)

## Resultado esperado

Ler intervalos com versão de conteúdo. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

operações de arquivo no runner; schemas de ferramentas. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Ler intervalos de linhas com números, versão/hash e tamanho total.
- [x] Devolver limites aplicados e referência para continuação.
- [x] Tratar encoding, arquivo ausente e binário com erro explícito.
- [x] Aplicar limite de resposta sem usar corte como se fosse arquivo completo.

## Critérios de aceite

- [x] Arquivo maior que 200 KB pode ser inspecionado em partes sem leitura integral no contexto.
- [x] Hash identifica exatamente o conteúdo observado para edição posterior.
- [x] Limites e linhas são consistentes com Unicode e arquivo sem newline final.
- [x] Leitura não revela caminhos do host nem arquivos externos à raiz.

## Validação e evidências exigidas

- [x] Testar arquivo grande, Unicode, CRLF, linhas vazias e ranges inválidos.
- [ ] Comparar paginação com conteúdo esperado e registrar tokens de entrada.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

workspace_read; path relativo, range, hash, bytes retornados e truncamento.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `7a6a00d` (`readRange`), `bd3ee7a` (ferramenta `workspace_read_range` e evento `workspace_read`). Caminhos: `packages/environments/src/workspace/ops.ts`, `packages/bots/src/programming/run-tools.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário, simulado e Docker real.
- Evidências automatizadas: `packages/environments/tests/workspace-ops.test.ts`: "inspects a file larger than 200 KB in parts with a hash of the exact content", "counts lines consistently for Unicode, CRLF, blank lines and a missing final newline", "fails explicitly for invalid ranges, missing, binary and non UTF-8 files", "never follows a symlink out of the worktree"; `workspace-extension.test.ts`: "reads by range with a hash and replaces with the hash as precondition", "authorizes every operation by project and hides other bots’ journals"; Docker real `workspace.e2e.test.ts`: "searches, reads by range, patches with preconditions and diffs inside the container". Comandos: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote); `OINKO_DOCKER_TEST=1 pnpm exec vitest run tests/docker.test.ts tests/runner.e2e.test.ts tests/failures.e2e.test.ts tests/browser.e2e.test.ts tests/workspace.e2e.test.ts tests/publication-docker.e2e.test.ts` (em `packages/environments`).
- Evidências reais / aceite de operador: Não aplicável — sem sistema externo nem aceite exigido.
- Limitações e riscos residuais: A comparação da paginação não registra tokens de entrada do modelo; o evento `workspace_read` registra bytes e truncamento.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
