# M02-S04 — Carregar instruções e descobrir comandos de monorepo

**Status:** in_progress — AGENTS.md com precedência, comandos com origem e diretório, overrides do projeto e texto do repositório como dado não confiável testados; faltam E2E da edição do override na dashboard e teste de mudança de instruções durante o run.

**Milestone:** [M02 — Entendimento do projeto e edição precisa](MILESTONE.md)
**Dependências:** [M02-S01](../M02/M02-S01.md), [M02-S02](../M02/M02-S02.md)
**Requisitos:** [R09](../REQUIREMENTS.md#r09), [R10](../REQUIREMENTS.md#r10), [R14](../REQUIREMENTS.md#r14)

## Resultado esperado

Carregar instruções e descobrir comandos de monorepo. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

packages/workspaces; context assembly; dashboard project configuration. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Resolver AGENTS.md da raiz até o arquivo alvo com precedência de escopo.
- [x] Consultar README e manifests pertinentes ao pacote, sem carregar todo monorepo.
- [x] Descobrir candidatos de instalação, teste, lint e build; salvar overrides por projeto/pacote.
- [x] Tratar texto de repositório como dados não confiáveis que não ampliam permissões.

## Critérios de aceite

- [x] Mudança em pacote aplica instruções locais e herdadas, com fonte visível.
- [ ] Override explícito do projeto prevalece sobre detecção e pode ser editado na dashboard.
- [x] Comando detectado informa origem e diretório de execução.
- [x] Instrução de README não consegue extrair credenciais ou autorizar publicação.

## Validação e evidências exigidas

- [x] Testar monorepo com comandos e AGENTS conflitantes em subpastas.
- [ ] Testar pacote sem manifest, mudança de instruções e conteúdo malicioso.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

project_instructions_resolved, project_commands_discovered; hashes, origem e escopo.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `7a6a00d` (`projectContext`), `bd3ee7a` (ferramenta `workspace_context` e eventos `project_instructions_resolved`/`project_commands_discovered`), `140cb12` (instruções fixadas entre ciclos), `6c77375` (comandos em `apps/dashboard/src/features/projects/programming-settings.tsx`). Caminhos: `packages/environments/src/workspace/ops.ts`, `packages/bots/src/programming/run-tools.ts`, `packages/agent-runtime/src/programming/agent-executor.ts`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário e simulado.
- Evidências automatizadas: `packages/environments/tests/workspace-ops.test.ts`: "resolves scoped AGENTS.md with precedence and discovers commands with their origin", "lets explicit project overrides win and handles packages without manifests", "returns malicious repository text as untrusted data only"; `workspace-extension.test.ts`: "uses project command overrides in the discovered context"; `packages/agent-runtime/tests/programming/agent-executor.test.ts`: "keeps project instructions pinned in later cycle prompts"; `packages/environments/tests/publication-policy.test.ts`: "requires an allowed publisher bot for every publication command; admin only reads status and installation"; `packages/bots/tests/programming-run.test.ts`: "fixes a bug proven by a failing test and completes only on evidence of the final revision" (eventos de instruções e comandos no journal). Comando: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote).
- Evidências reais / aceite de operador: Não aplicável — sem sistema externo nem aceite exigido.
- Limitações e riscos residuais: A edição de overrides existe na tela de projeto sem E2E próprio. Mudança de instruções durante o run não é testada (o hash é registrado, mas não há teste de invalidação ou releitura). A detecção de `build` não é afirmada no teste (só test, lint e install).
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
