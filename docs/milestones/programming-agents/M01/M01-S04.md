# M01-S04 — Contabilizar tokens, custo e tempos por run

**Status:** in_progress — uso por ID de chamada, custo desconhecido, reconciliação tardia e agregação testados; o serviço grava apenas os intervalos de fila e total (contexto, modelo e ferramenta não são registrados em produção) e a UI de custo confirmado vs. estimado não tem teste.

**Milestone:** [M01 — Telemetria profunda desde a fundação](MILESTONE.md)
**Dependências:** [M01-S01](../M01/M01-S01.md), [M01-S02](../M01/M01-S02.md)
**Requisitos:** [R06](../REQUIREMENTS.md#r06), [R14](../REQUIREMENTS.md#r14), [R15](../REQUIREMENTS.md#r15)

## Resultado esperado

Contabilizar tokens, custo e tempos por run. A capacidade pertence à plataforma e deve funcionar com qualquer bot autorizado.

## Áreas previstas

SDK usage; decision usage; agregação de runs; dashboard. Caminhos definitivos serão confirmados na implementação; são áreas de responsabilidade, não arquivos já entregues.

## Trabalho a construir

- [x] Atribuir tokens de principal, fast, Jev, resumo, retries e fallback ao mesmo run.
- [ ] Separar fila, preparação de contexto, espera do modelo, execução de ferramenta e duração total.
- [x] Preservar pending/confirmed/unavailable e reconciliar dados posteriores do provedor.
- [x] Agregar por run, bot, projeto, modelo e versão de política sem impor teto financeiro.

## Critérios de aceite

- [x] Custo desconhecido nunca aparece como zero; cobertura de custo é exibida.
- [x] Soma usa IDs de chamadas e contabiliza uma tentativa abortada quando houver uso reportado.
- [x] Tempo total não é soma ingênua de spans sobrepostos.
- [ ] UI distingue custo confirmado de qualquer estimativa identificada; gasto não cancela tarefa.

## Validação e evidências exigidas

- [x] Testar duplicação de eventos, uso tardio, chamadas paralelas e custo ausente.
- [x] Conferir agregação contra fatura sintética e tempos controlados.
- [x] Aplicar os gates comuns de [validação](../VALIDATION.md), registrando comando, revisão, resultado e limitações.
- [x] Executar verificações negativas de permissão pertinentes; comprovar ausência de dependência de ID/nome do bot.

## Telemetria obrigatória

usage_reported, usage_reconciled, run_metrics_updated.

Aplicar o [contrato transversal](../TELEMETRY.md): correlação, versão efetiva, tentativas, duração, resultado/erro, redaction, acesso e retenção. Para operações com efeito, persistir intenção e recibo; resultado incerto deve ser reconciliado. Testes devem consultar os eventos persistidos, não apenas verificar que um callback foi chamado.

## Falhas e compatibilidade

Usar os estados e regras de [arquitetura](../ARCHITECTURE.md). Falhas mantêm evidência, não ampliam permissões nem apagam trabalho do usuário. Interfaces existentes continuam compatíveis; ativação segue capacidade/política do bot e do projeto.

## Registro de entrega

- Implementação/PR: Commits locais no branch `feat/programming-agents`, sem PR: `c337590` (UsageLedger e intervalos), `bd3ee7a` (coleta da telemetria do SDK por run), `3b61cf7` (tentativa cancelada do fallback contabilizada). Caminhos: `packages/agent-runtime/src/programming/usage.ts`, `packages/agent-runtime/src/programming/service.ts`, `packages/oinko/src/telemetry/llm-call-record.ts`, `apps/dashboard/src/features/programming/run-detail.tsx`.
- Revisão e ambiente testados: Revisão `60fe1f9` (código de produto igual ao de `835dcb5`), relatório de validação em `validation-reports/` (`validation-60fe1f978cf9.md`: build, typecheck, lint, test, suítes por pacote, gate de IDs de piloto, Docker real — 1 skipped, Railpack — e Playwright 39/39 aprovados; na execução anterior, em `be30ad6`, houve 1 falha em `workspaces.spec.ts`, corrigida em `835dcb5`). macOS + Docker Desktop, Node v26.8.1 (requisito 22.5+). Ambientes: unitário e simulado.
- Evidências automatizadas: `packages/agent-runtime/tests/programming/usage.test.ts`: "sums by call id so redelivered reports never double tokens or cost", "never shows unknown cost as zero and reports coverage", "reconciles late usage and confirmed cost once, and counts aborted attempts that reported usage", "measures total time as a union of overlapping spans and separates phases", "matches a synthetic invoice when aggregated by bot, model and policy", "collects main, fast, summary and Jev calls of a run from the SDK telemetry database"; `packages/bots/tests/programming-run.test.ts`: "fixes a bug proven by a failing test and completes only on evidence of the final revision" (uso atribuído ao run por chamada); `packages/agent-runtime/tests/programming/evaluation.test.ts`: "never turns unknown cost into zero"; `packages/bots/tests/pilot.test.ts` (runs e métricas de outro bot não visíveis). Comando: `pnpm validate:programming` (`node scripts/validation-suite.mjs`: `pnpm build`, `pnpm typecheck`, `pnpm lint`, `pnpm test` e `pnpm exec vitest run` por pacote).
- Evidências reais / aceite de operador: Pendente — reconciliação com custo informado por provedor real não exercitada (simulações não informam custo).
- Limitações e riscos residuais: `service.ts` registra apenas os intervalos `queue` e `total`; `context`, `model` e `tool` existem no ledger e no teste, mas não são gravados durante a execução. Agregação por projeto usa o mesmo caminho testado para bot/modelo/política, sem teste direto. Nenhum teste de UI verifica a distinção entre custo confirmado e estimado no detalhe do run.
- Não alterar para concluída apenas porque código compilou ou testes simulados passaram.
